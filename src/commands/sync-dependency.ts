export function syncDependency(repository: string, dependencyName: string, dependencyVersion: string) {
  console.log(`syncing dependency ${dependencyName} to version ${dependencyVersion} in repository ${repository}`)
}
